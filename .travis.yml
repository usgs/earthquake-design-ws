sudo: false
language: php
php:
  - '5.6'
env:
  - NON_INTERACTIVE=true
before_install:
  - composer self-update
  - nvm install 4.5.0
  - nvm use 4.5.0
  - npm --version
  - node --version
  - npm list -g
install:
  - npm install
script:
  - npm test
